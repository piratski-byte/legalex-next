const SingleLink = ({ title }: { title: string }) => {
  return (
    <div
      className="border-b-2 border-transparent transition-all hover:border-blue_light cursor-pointer"
      onClick={() => {}}
    >
      {title}
    </div>
  );
};

export default SingleLink;
